name: Customer Helpdesk Workspace
route: /helpdesk
type: workspace
layout: two_panel
label: Agent and SLA Queue
module: Customer Helpdesk
components:
  - id: primary_queue
    type: record_table
    region: main
    position: 0
    source_doctype: Ticket
    label: Agent and SLA Queue
    desktop_columns: [name, status, modified]
    mobile_columns: [name, status]
  - id: exception_queue
    type: record_table
    region: side
    position: 0
    source_doctype: Ticket
    label: Exceptions
    desktop_columns: [name, status, modified]
    mobile_columns: [name, status]
