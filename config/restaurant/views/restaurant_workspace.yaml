name: Restaurant and Food Service Workspace
route: /restaurant
type: workspace
layout: two_panel
label: Restaurant and Food Service
module: Restaurant and Food Service
components:
  - id: primary_queue
    type: record_table
    region: main
    position: 0
    source_doctype: Food Order
    label: Food Order Queue
    desktop_columns: [name, reference, owner, status, due_date]
    mobile_columns: [reference, status]
  - id: evidence_queue
    type: record_table
    region: side
    position: 0
    source_doctype: Production Ticket
    label: Production Ticket Queue
    desktop_columns: [name, reference, source_record, status]
    mobile_columns: [reference, status]
